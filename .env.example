# ============================================================================
# DELTA EXCHANGE API CREDENTIALS & BOT CONFIGURATION
# ============================================================================

# API Credentials from Delta Exchange (Get from API Keys page on Delta Exchange)
# For Delta Exchange India, use https://api.india.delta.exchange or https://api.delta.exchange
DELTA_API_KEY="your_api_key_here"
DELTA_API_SECRET="your_api_secret_here"
DELTA_BASE_URL="https://api.india.delta.exchange"

# Trading Settings
SYMBOL="BTCUSD"          # Trading contract symbol on Delta Exchange (e.g. BTCUSD, BTCUSDT)
TIMEFRAME="4h"           # Timeframe for Trend Rider algorithm (4h)
RISK_PCT="1.5"           # Risk % per trade (default: 1.5%)
LEVERAGE="5"             # Leverage multiplier to set on Delta (e.g., 5, 10)

# Trailing Stop Settings (User Configured)
TRAIL_PCT_ACTIVATION="1.0"  # Move 1.0% in profit activates trailing stop
TRAIL_PCT_DISTANCE="0.4"    # Trail 0.4% behind peak price

# Operational Mode
DRY_RUN="true"           # Set to 'false' for real live order execution; 'true' for paper trading
POLL_INTERVAL_SEC="60"   # How often bot checks for candle close and updates trailing stops
